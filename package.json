{
  "name": "pnm-browser-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-side bridge between WebAuthn passkeys and VTA-managed DIDs. Ships as a PWA and as an MV3 browser extension on a shared TypeScript core.",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/pwa",
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:pwa": "npm run dev --workspace @pnm/pwa",
    "dev:extension": "npm run dev --workspace @pnm/extension",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
