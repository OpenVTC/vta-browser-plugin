// Verifiable Trust Community — the member's side of `vtc/*`.
//
// Applying to a community, tracking the application, holding the membership
// credential, and leaving. The community's own administration surface (
// endorsements, invitations, member management, its website) is implemented by
// a VTC rather than by an agent, and is not wrapped here.

export * from "./membership.js";
